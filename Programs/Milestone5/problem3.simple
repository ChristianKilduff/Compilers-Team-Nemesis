n = 0.0

input decimal n

steps = 0.0
max_val = n

one = 1.0
two = 2.0
three = 3.0
while n not equal to one {
    is n greater than max_val{
        max_val = n
    }

    m =0.0
    * m = n mod two TODO
    is m equal to 0.0 {
        n = n divide two
    } if not{
        n = three multiply n
        n = n plus one 
    }

    steps = steps plus one 
}

is n greater than max_val{
    max_val = n
}

print "steps = " inline
print steps
print "max = " inline
print max_val